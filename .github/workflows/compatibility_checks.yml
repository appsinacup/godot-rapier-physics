name: 🧩 Compatibility Checks
on:
  workflow_call:

jobs:
  cargo-check:
    name: Godot API ${{ matrix.api.version }} ${{ matrix.dimensions.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        api:
          - { version: "4.4", feature: "api-4-4" }
          - { version: "4.5", feature: "api-4-5" }
          - { version: "4.6", feature: "api-4-6" }
          - { version: "4.7", feature: "api-4-7" }
        dimensions:
          - { name: 2D, feature: "single-dim2" }
          - { name: 3D, feature: "single-dim3" }

    steps:
      - uses: actions/checkout@v6

      - name: Cargo Check
        shell: bash
        run: |
          set -euo pipefail
          features="${{ matrix.dimensions.feature }},serde-serialize,parallel,register-docs,${{ matrix.api.feature }}"
          cargo check --features="$features"
